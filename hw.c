#include <stdio.h>
#include <stdlib.h> 
#include <string.h>
#include <stdbool.h>

#define MAX_MAIN 32
#define MAX_VIRTUAL 64

int main(int argc, char *argv[]) {
    char *main_mem[MAX_MAIN] = {"-1"};
    char *virtual_mem[MAX_VIRTUAL] = {"-1"};

    if(argc > 1) {
        
    }
    else {

    }


    return 0;
}